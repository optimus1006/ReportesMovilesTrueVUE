<div class="table">
  <table>
    <thead>
      <tr>
        <%= raw theaders(thead) %>
      </tr>
    </thead>
    <tbody>
        <%= raw tbody(rows, colums) %>
    </tbody>
  </table>
</div>

<%= render "reports/pagination", :total => total, :current => current %>